package com.hospital.jpa;

import org.springframework.data.jpa.repository.JpaRepository;

import com.hospital.model.ServiceMasterSpeciality;

public interface ServiceMasterSpecialityRepository extends JpaRepository<ServiceMasterSpeciality, Long> {
}
